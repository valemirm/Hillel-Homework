1. Яким буде значення змінної x після завершення циклу for (int x = 0; x < 100; x++)?

х = 100

6. Баги: знайдіть помилку у наведеному фрагменті програми.
- рядок 11 у даному редакторі, невірне ім'я змінної "counetr" та відсутність символу ; в кінці рядка

int counter = 0;
while (counter < 10)
{
	cout << "counter: " << counetr:
}

7. Баги: знайдіть помилку у наведеному фрагменті програми.
- рядок 18, зайва ; після круглих дужок, через це наступний рядок вже не належить циклу for
- у оголошенні параметрів for після counter < 10 має йти ; а не :

for (int counter = 0; counter < 10: counter++);
	cout << counter << " ";

8. Баги: знайдіть помилку у наведеному фрагменті програми.
- логічний вираз в дужках умови while має містити counter > 10 замість знаку <, оскільки за даних умов вираз буде false та цикл не запуститься
- не баг, а примітка: додати "\n" або endl в кінці рядку 28, інакше виведений текст буде злитий в рядок

int counter = 100;
while (counter < 10)
{
	cout << "counter now: " << counter;
	counter--;
}

